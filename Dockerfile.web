FROM nginx:alpine AS runner

COPY nginx.conf /etc/nginx/conf.d/default.conf

# Ship the prebuilt SPA from the host (fast; avoids re-copying OneDrive trees).
COPY dist /usr/share/nginx/html

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]